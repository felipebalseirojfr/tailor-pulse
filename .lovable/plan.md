Adicionar um botão "Voltar" no topo da página Comercial (`src/pages/Comercial.tsx`), ao lado do título, que navega para `/` (home).

- Ícone `ArrowLeft` do lucide-react
- Variante `ghost`, tamanho pequeno
- Usa `useNavigate` do react-router-dom